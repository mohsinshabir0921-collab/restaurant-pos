# POS SPA fallback: rewrite every non-asset path to /index.html so POS
# (BrowserRouter) routes like /login and /dashboard keep working on refresh.
# Static files take precedence over this rewrite, so /website.html and all
# /assets/* still serve directly; the public website's HashRouter routes
# (#/...) never hit the server and need no rewrite.
/*  /index.html  200
